To run tests without generating a report, use this command:
pytest -v

To run tests with generation of a simple HTML report you use this command:
pytest --html=report.html --self-contained-html

There will be a screenshot taken on a case of test failure and screenshot will be added to screenshots folder